<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <startup>
    <supportedRuntime version="v4.0.30319"/>
  </startup>
  <runtime>
    <!-- Skip Authenticode publisher-evidence generation at startup.  Without
         this, the CLR can stall for seconds while Windows checks certificate
         revocation lists (CRLs), especially on machines that are offline or
         behind a slow proxy.  Safe to disable for a desktop editor. -->
    <generatePublisherEvidence enabled="false"/>
    <!-- Concurrent (background) GC keeps the UI responsive; it is the default
         for workstation apps, stated here explicitly. -->
    <gcConcurrent enabled="true"/>
  </runtime>
</configuration>
